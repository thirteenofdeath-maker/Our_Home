import Link from"next/link";import{Card}from"@/components/ui/Card";import{formatCurrency}from"@/lib/utils/money";import type{SavingGoalSummary}from"../types";export function GoalCard({goal}:{goal:SavingGoalSummary}){return<Link href={`/finance/goals/${goal.goalId}`}><Card className="flex flex-col gap-2"><div className="flex justify-between"><span className="font-medium">{goal.name}</span><span>{goal.progressPercent}%</span></div><p className="text-sm">{formatCurrency(goal.progressAmount,goal.currency)} / {formatCurrency(goal.targetAmount,goal.currency)}</p><div className="h-2 overflow-hidden rounded-full bg-surface-muted"><div className="h-full bg-primary" style={{width:`min(max(${goal.progressPercent}%, 0%), 100%)`}}/></div>{goal.pocketArchived||goal.walletArchived?<p className="text-xs text-danger">Pocket/Wallet ถูกเก็บถาวร</p>:null}</Card></Link>}
