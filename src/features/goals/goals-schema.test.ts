import{readFileSync}from"node:fs";import{resolve}from"node:path";import{describe,expect,it}from"vitest";const sql=readFileSync(resolve(process.cwd(),"supabase/migrations/0039_saving_goals.sql"),"utf8");describe("linked Pocket goals",()=>{it("stores no mutable progress and reads ledger balance",()=>{expect(sql).not.toMatch(/current_amount/);expect(sql).toContain("public.get_pocket_balance(p.id)")});it("allows at most one active goal per Pocket",()=>{expect(sql).toMatch(/unique index saving_goals_one_active_per_pocket_idx[\s\S]*where archived_at is null/)});it("preserves unclamped progress while deriving presentation values",()=>{expect(sql).toContain("bal.amount,greatest");expect(sql).toContain("bal.amount>=g.target_amount")})});
