import"server-only";import type{SupabaseClient}from"@supabase/supabase-js";import type{Database}from"@/types/database";import{normalizeDatabaseMoney}from"@/lib/utils/money";export interface Insight{type:string;currency:string;currentAmount:string;comparisonAmount:string|null;referenceId:string|null;label:string}export async function getInsights(s:SupabaseClient<Database>,args:{currentStart:string;currentEnd:string;previousStart:string;today:string}):Promise<Insight[]>{const{data,error}=await s.rpc("get_finance_insights",{p_current_start:args.currentStart,p_current_end:args.currentEnd,p_previous_start:args.previousStart,p_today:args.today});if(error)throw error;return((data??[])as Array<{insight_type:string;currency:string;current_amount:string|number;comparison_amount:string|number|null;reference_id:string|null;label:string}>).map(x=>({type:x.insight_type,currency:x.currency,currentAmount:normalizeDatabaseMoney(x.current_amount),comparisonAmount:x.comparison_amount==null?null:normalizeDatabaseMoney(x.comparison_amount),referenceId:x.reference_id,label:x.label}))}
