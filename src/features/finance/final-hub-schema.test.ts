import{readFileSync}from"node:fs";import{resolve}from"node:path";import{describe,expect,it}from"vitest";const sql=readFileSync(resolve(process.cwd(),"supabase/migrations/0049_finance_hub_final.sql"),"utf8");describe("final Finance Hub",()=>{it("reuses net worth and reports",()=>{expect(sql).toContain("get_net_worth()");expect(sql).toContain("get_finance_reports('PERSONAL'")});it("batches upcoming installments",()=>{expect(sql).toContain("installment_occurrences");expect(sql).toContain("limit 3")});it("uses security invoker RLS",()=>{expect(sql).toContain("security invoker")})});
