import{readFileSync}from"node:fs";import{resolve}from"node:path";import{describe,expect,it}from"vitest";const sql=readFileSync(resolve(process.cwd(),"supabase/migrations/0044_finance_attachments.sql"),"utf8");describe("Finance attachments",()=>{it("keeps bucket private with 15 MiB cap and fixed MIME list",()=>{expect(sql).toContain("false,15728640");expect(sql).toContain("'application/pdf'")});it("authorizes metadata and objects through transaction access",()=>{expect(sql.match(/is_transaction_authorized/g)?.length).toBeGreaterThanOrEqual(5)});it("uses stable transaction-owned paths",()=>{expect(sql).toContain("storage_path~('^'||transaction_id::text")})});
