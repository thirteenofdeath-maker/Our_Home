"use client";import{useActionState}from"react";import{SubmitButton}from"@/components/ui/SubmitButton";import{initialActionState}from"@/lib/types/action-state";import{uploadTransactionAttachmentAction}from"./actions";export function AttachmentForm({transactionId}:{transactionId:string}){const[state,action]=useActionState(uploadTransactionAttachmentAction,initialActionState);return <form action={action} className="flex flex-col gap-2"><input type="hidden" name="transactionId" value={transactionId}/><input name="file" type="file" required accept="image/jpeg,image/png,image/webp,application/pdf"/><p className="text-xs text-foreground-muted">JPEG, PNG, WebP หรือ PDF · สูงสุด 15 MB</p>{state.error?<p className="text-danger">{state.error}</p>:null}<SubmitButton>แนบหลักฐาน</SubmitButton></form>}
