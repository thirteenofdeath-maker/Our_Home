import"server-only";import type{SupabaseClient}from"@supabase/supabase-js";import type{Database}from"@/types/database";
export interface TransactionAttachment{id:string;transactionId:string;fileName:string;mimeType:string;fileSize:number;url:string}
export async function listTransactionAttachments(s:SupabaseClient<Database>,transactionId:string):Promise<TransactionAttachment[]>{const{data,error}=await s.from("transaction_attachments").select("id,transaction_id,storage_path,mime_type,file_name,file_size").eq("transaction_id",transactionId).order("created_at");if(error)throw error;return Promise.all((data??[]).map(async x=>{const signed=await s.storage.from("finance-attachments").createSignedUrl(x.storage_path,3600);return{id:x.id,transactionId:x.transaction_id,fileName:x.file_name,mimeType:x.mime_type,fileSize:x.file_size,url:signed.data?.signedUrl??""}}))}
