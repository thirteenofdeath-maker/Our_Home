import{readFileSync}from"node:fs";import{resolve}from"node:path";import{describe,expect,it}from"vitest";const sql=readFileSync(resolve(process.cwd(),"supabase/migrations/0047_net_worth.sql"),"utf8");describe("net worth",()=>{it("uses exact asset plus receivable minus liability formula",()=>{expect(sql).toContain("coalesce(w.amount,0)+coalesce(d.receivables,0)-coalesce(d.liabilities,0)")});it("excludes voided ledger/debt events",()=>{expect(sql.match(/t.deleted_at is null/g)).toHaveLength(2)});it("groups every source by currency",()=>{expect(sql.match(/group by currency/g)?.length).toBeGreaterThanOrEqual(2)});it("does not hide archived balances",()=>{expect(sql).not.toContain("archived_at is null")})});
