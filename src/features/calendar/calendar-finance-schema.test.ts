import{readFileSync}from"node:fs";import{resolve}from"node:path";import{describe,expect,it}from"vitest";const sql=readFileSync(resolve(process.cwd(),"supabase/migrations/0043_calendar_finance_composition.sql"),"utf8");describe("Calendar Finance composition",()=>{it("reads all due sources without inserting calendar events",()=>{expect(sql).toContain("recurring_occurrences");expect(sql).toContain("bill_occurrences");expect(sql).toContain("installment_occurrences");expect(sql).not.toMatch(/insert into public\.calendar_events/i)});it("uses source RLS through security invoker",()=>{expect(sql).toContain("security invoker")});it("bounds canonical dates",()=>{expect(sql.match(/due_date>=p_start/g)).toHaveLength(3);expect(sql.match(/due_date<p_end/g)).toHaveLength(3)})});
