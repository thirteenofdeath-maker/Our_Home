import{readFileSync}from"node:fs";import{resolve}from"node:path";import{describe,expect,it}from"vitest";const sql=readFileSync(resolve(process.cwd(),"supabase/migrations/0042_finance_reports.sql"),"utf8");describe("finance reports",()=>{it("aggregates only Income and Expense in PostgreSQL",()=>{expect(sql).toContain("transaction_type in('INCOME','EXPENSE')");expect(sql).toContain("sum(amount)");expect(sql).toContain("sum(-amount)")});it("excludes voided rows and uses occurred_at",()=>{expect(sql).toContain("t.deleted_at is null");expect(sql).toContain("t.occurred_at>=p_start")});it("groups unlike currencies separately",()=>{expect(sql).toMatch(/months as[\s\S]*group by 1,currency/);expect(sql).toMatch(/categories as[\s\S]*group by b\.transaction_type,b\.category_id,c\.name,b\.currency/)});it("uses a safe SQL alias while preserving the month JSON key",()=>{expect(sql).toContain("as month_key");expect(sql).toContain("'month',month_key");expect(sql).not.toMatch(/\)month[,\s]/)});it("uses Bangkok calendar months",()=>{expect(sql).toContain("Asia/Bangkok")})});
