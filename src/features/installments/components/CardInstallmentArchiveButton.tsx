"use client";import{useActionState}from"react";import{SubmitButton}from"@/components/ui/SubmitButton";import{initialActionState}from"@/lib/types/action-state";import{setCardInstallmentArchivedAction}from"../actions";
export function CardInstallmentArchiveButton({planId,archived}:{planId:string;archived:boolean}){const[state,action]=useActionState(setCardInstallmentArchivedAction,initialActionState);return<form action={action}><input type="hidden" name="planId" value={planId}/><input type="hidden" name="archived" value={archived?"false":"true"}/><SubmitButton variant="secondary">{archived?"กู้คืนแผน":"เก็บแผนถาวร"}</SubmitButton>{state.error?<p role="alert" className="mt-2 text-sm text-danger">{state.error}</p>:null}</form>}
