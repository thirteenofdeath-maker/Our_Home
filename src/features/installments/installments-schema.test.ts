import{readFileSync}from"node:fs";import{resolve}from"node:path";import{describe,expect,it}from"vitest";const sql=readFileSync(resolve(process.cwd(),"supabase/migrations/0038_installments.sql"),"utf8");describe("installment SQL",()=>{it("distributes decimal remainder into final installment",()=>{expect(sql).toContain("trunc((new.total_amount*100)/new.installment_count)/100");expect(sql).toContain("new.total_amount-(base*(new.installment_count-1))")});it("creates no ledger until atomic locked payment",()=>{expect(sql).toMatch(/pay_installment_occurrence[\s\S]*for update/);expect(sql).toContain("create_income_expense_transaction('EXPENSE'")});it("freezes schedule and enforces exact V1 payment",()=>{expect(sql).toContain("V1 payment must equal expected installment amount");expect(sql).toContain("installment_plans_freeze_schedule")})});
