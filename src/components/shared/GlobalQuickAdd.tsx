import Link from"next/link";import{financeExpenseHref,financeIncomeHref,financeTransferHref}from"@/features/finance/domain/finance";export function GlobalQuickAdd({walletId}:{walletId:string|null}){return <Link aria-label="เพิ่มรายการการเงิน" href={walletId?`/finance/quick-add?walletId=${walletId}`:"/wallets/new"} className="fixed bottom-20 right-4 z-20 flex size-14 items-center justify-center rounded-full bg-primary text-2xl text-primary-foreground shadow-lg">+</Link>}export function QuickAddChoices({walletId}:{walletId:string}){return <div className="grid gap-3"><Link href={financeIncomeHref(walletId)} className="min-h-12 rounded-control bg-surface p-4 text-income">+ รายรับ</Link><Link href={financeExpenseHref(walletId)} className="min-h-12 rounded-control bg-surface p-4 text-expense">− รายจ่าย</Link><Link href={financeTransferHref(walletId)} className="min-h-12 rounded-control bg-surface p-4 text-primary">↔ โอนเงิน</Link></div>}
