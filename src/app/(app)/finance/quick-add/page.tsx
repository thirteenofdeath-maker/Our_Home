import{notFound}from"next/navigation";import{QuickAddChoices}from"@/components/shared/GlobalQuickAdd";import{getWallet}from"@/features/wallets/api";import{requireUser}from"@/lib/auth/require-user";export default async function Page({searchParams}:{searchParams:Promise<{walletId?:string}>}){const q=await searchParams;const{supabase}=await requireUser();if(!q.walletId)notFound();const wallet=await getWallet(supabase,q.walletId);if(!wallet||wallet.is_archived)notFound();return <div className="flex flex-col gap-4"><h1 className="text-xl font-semibold">เพิ่มรายการ · {wallet.name}</h1><QuickAddChoices walletId={wallet.id}/></div>}
