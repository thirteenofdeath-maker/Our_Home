import Link from"next/link";import{buttonClassName}from"@/components/ui/Button";import{Card}from"@/components/ui/Card";import{listDebts}from"@/features/debts/api";import{requireUser}from"@/lib/auth/require-user";import{formatCurrency}from"@/lib/utils/money";export default async function Page(){const{supabase}=await requireUser();const rows=await listDebts(supabase,"PERSONAL");return<div className="flex flex-col gap-4"><h1 className="text-xl font-semibold">หนี้และเงินยืม</h1><Link href="/finance/debts/new" className={buttonClassName("primary","lg")}>+ เพิ่ม</Link>{rows.map(x=><Link key={x.id} href={`/finance/debts/${x.id}`}><Card className="flex justify-between"><span>{x.name} · {x.debtType}</span><span>{formatCurrency(x.outstanding,x.currency)}</span></Card></Link>)}</div>}
