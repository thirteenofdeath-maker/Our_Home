import{notFound}from"next/navigation";import{getInstallmentOccurrence,getInstallmentPlan}from"@/features/installments/api";import{PayInstallmentForm}from"@/features/installments/components/PayInstallmentForm";import{listMyWallets}from"@/features/wallets/api";import{listPocketsForWallet}from"@/features/pockets/api";import{requireUser}from"@/lib/auth/require-user";export default async function Page({params}:{params:Promise<{occurrenceId:string}>}){const{occurrenceId}=await params;const{supabase}=await requireUser();const o=await getInstallmentOccurrence(supabase,occurrenceId);if(!o||o.status!=="OPEN")notFound();const p=await getInstallmentPlan(supabase,o.planId);if(!p)notFound();const wallets=(await listMyWallets(supabase)).filter(w=>w.scope===p.scope&&w.currency===p.currency);const pairs=await Promise.all(wallets.map(async w=>[w.id,await listPocketsForWallet(supabase,w.id)]as const));return<div className="flex flex-col gap-4"><h1 className="text-xl font-semibold">จ่ายงวด {o.sequenceNumber}: {p.name}</h1><PayInstallmentForm id={o.id} amount={o.expectedAmount} categoryId={p.categoryId} wallets={wallets} pockets={Object.fromEntries(pairs)}/></div>}
