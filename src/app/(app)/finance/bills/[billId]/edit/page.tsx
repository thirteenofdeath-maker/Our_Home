import { notFound } from "next/navigation";import { getBill } from "@/features/bills/api";import { BillForm } from "@/features/bills/components/BillForm";import { listCategories } from "@/features/categories/api";import { buildCategoryTree } from "@/features/categories/domain/tree";import { getMyPrimaryHousehold } from "@/features/household/api";import { listPocketsForWallet } from "@/features/pockets/api";import { listTags } from "@/features/tags/api";import { listMyWallets } from "@/features/wallets/api";import { requireUser } from "@/lib/auth/require-user";
export default async function EditBillPage({params}:{params:Promise<{billId:string}>}){const {billId}=await params;const {supabase,user}=await requireUser();const bill=await getBill(supabase,billId);if(!bill)notFound();const h=await getMyPrimaryHousehold(supabase,user.id);const [wallets,pc,hc,pt,ht]=await Promise.all([listMyWallets(supabase),listCategories(supabase,{scope:"PERSONAL",transactionType:"EXPENSE"}),h?listCategories(supabase,{scope:"HOUSEHOLD",transactionType:"EXPENSE"}):Promise.resolve([]),listTags(supabase,{scope:"PERSONAL"}),h?listTags(supabase,{scope:"HOUSEHOLD",householdId:h.id}):Promise.resolve([])]);const pairs=await Promise.all(wallets.map(async w=>[w.id,await listPocketsForWallet(supabase,w.id)] as const));return <div className="flex flex-col gap-4"><h1 className="text-xl font-semibold">แก้ไขบิล</h1><BillForm bill={bill} wallets={wallets} pocketsByWallet={Object.fromEntries(pairs)} categories={{PERSONAL:buildCategoryTree(pc),HOUSEHOLD:buildCategoryTree(hc)}} tags={{PERSONAL:pt,HOUSEHOLD:ht}} hasHousehold={Boolean(h)}/></div>}
