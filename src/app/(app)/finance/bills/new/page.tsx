import { listBills } from "@/features/bills/api";import { BillForm } from "@/features/bills/components/BillForm";import { listCategories } from "@/features/categories/api";import { buildCategoryTree } from "@/features/categories/domain/tree";import { getMyPrimaryHousehold } from "@/features/household/api";import { listPocketsForWallet } from "@/features/pockets/api";import { listTags } from "@/features/tags/api";import { listMyWallets } from "@/features/wallets/api";import { requireUser } from "@/lib/auth/require-user";
export default async function NewBillPage(){const {supabase,user}=await requireUser();const household=await getMyPrimaryHousehold(supabase,user.id);const [wallets,pc,hc,pt,ht]=await Promise.all([listMyWallets(supabase),listCategories(supabase,{scope:"PERSONAL",transactionType:"EXPENSE"}),household?listCategories(supabase,{scope:"HOUSEHOLD",transactionType:"EXPENSE"}):Promise.resolve([]),listTags(supabase,{scope:"PERSONAL"}),household?listTags(supabase,{scope:"HOUSEHOLD",householdId:household.id}):Promise.resolve([])]);const pairs=await Promise.all(wallets.map(async w=>[w.id,await listPocketsForWallet(supabase,w.id)] as const));void listBills;return <div className="flex flex-col gap-4"><h1 className="text-xl font-semibold">เพิ่มบิล</h1><BillForm wallets={wallets} pocketsByWallet={Object.fromEntries(pairs)} categories={{PERSONAL:buildCategoryTree(pc),HOUSEHOLD:buildCategoryTree(hc)}} tags={{PERSONAL:pt,HOUSEHOLD:ht}} hasHousehold={Boolean(household)}/></div>}
